# Cloudflare Pages — canonical redirect rules

# Specific overrides FIRST (rules are first-match)
/index.html / 301
/portfolio /projects 301
/portfolio.html /projects 301

# Strip .html extension — explicit 301 for all pages
# Covers any .html URL Google finds via old links or backlinks
/*.html /:splat 301
